import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.swift.drop',
  appName: 'SwiftDrop',
  webDir: 'dist',
  bundledWebRuntime: false,
  plugins: {
    WifiP2P: {
      enabled: true
    }
  }
};

export default config;
